KERBALISM_RESOURCE_AUDIT_SETTINGS
{
	// Run one static scan after the part database is ready (main menu).
	Enabled = true

	// Also print the log folder path in the KSP console.
	LogToUnity = true

	// Stock / mod PartModule names that consume or produce resources outside Kerbalism brokers.
	// Remove a line to drop it from the default set when you replace the whole list via MM.
	// SuspiciousModule = ModuleResourceConverter
	// SuspiciousModule = ModuleResourceHarvester

	// Kerbalism / Bridge modules that mean the part is integrated (do not report these modules).
	// IntegrationModule = ProcessController
	// IntegrationModuleSuffix = KerbalismUpdater

	// nativeModule|UpdaterModule — native module is OK when the updater is on the same part.
	// NativeModuleUpdaterPair = ModuleSystemHeatConverter|SystemHeatConverterKerbalismUpdater

	// Skip entire mod folders (GameData top-level folder name).
	// IgnoreMod = Squad

	// Skip individual part names.
	// IgnorePart = somePartName
}
